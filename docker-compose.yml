services:

  # 🔐 Agent Gateway - JWT AuthN + RBAC AuthZ for all MCP calls
  agent-gateway:
    build:
      context: ./agent_gateway
      dockerfile: Dockerfile
    container_name: agent-gateway
    ports:
      - "8090:8090"
    env_file: .env
    environment:
      - BIND_ADDR=0.0.0.0:8090
      - RUST_LOG=info
      - JWT_SECRET=${JWT_SECRET:-dev-secret-change-in-production-minimum-32-chars}
      - GREEN_AGENT_API_KEY=${AGENT_API_KEY:-green-agent-dev-key-change-in-prod}
      - PURPLE_AGENT_API_KEY=purple-agent-dev-key-change-in-prod
      - S3_MCP_URL=http://s3-mcp:8081
      - AI_MCP_URL=http://ai-mcp:8082
      - COBOL_MCP_URL=http://cobol-mcp:8083
      - RUST_MCP_URL=http://rust-mcp:8084
    networks:
      - modernization-network
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/8090' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # 🪣 S3 MCP - AWS S3 operations
  s3-mcp:
    build:
      context: ./s3_mcp
      dockerfile: Dockerfile
    container_name: s3-mcp
    ports:
      - "8086:8081"
    env_file: .env
    environment:
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=us-east-1
      - BIND_ADDR=0.0.0.0:8081
      - RUST_LOG=info
    depends_on:
      - agent-gateway
    networks:
      - modernization-network
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/8081' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # 🧠 ai MCP - AI translation via Claude claude-opus-4-6
  ai-mcp:
    build:
      context: ./ai_mcp
      dockerfile: Dockerfile
    container_name: ai-mcp
    ports:
      - "8087:8082"
    env_file: .env
    environment:
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - BIND_ADDR=0.0.0.0:8082
      - RUST_LOG=info
    depends_on:
      - agent-gateway
    networks:
      - modernization-network
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/8082' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ⚙️ COBOL MCP - GnuCOBOL compilation and execution
  cobol-mcp:
    build:
      context: ./cobol_mcp
      dockerfile: Dockerfile
    container_name: cobol-mcp
    ports:
      - "8088:8083"
    environment:
      - BIND_ADDR=0.0.0.0:8083
      - RUST_LOG=info
    depends_on:
      - agent-gateway
    networks:
      - modernization-network
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/8083' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # 🦀 Rust MCP - Cargo compilation and execution
  rust-mcp:
    build:
      context: ./rust_mcp
      dockerfile: Dockerfile
    container_name: rust-mcp
    ports:
      - "8089:8084"
    environment:
      - BIND_ADDR=0.0.0.0:8084
      - RUST_LOG=info
    depends_on:
      - agent-gateway
    networks:
      - modernization-network
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/8084' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # 🟣 Purple Agent - AI Modernizer
  purple-agent:
    build:
      context: ./purple_agent
      dockerfile: Dockerfile
    container_name: purple-agent
    ports:
      - "8085:8081"
    env_file: .env
    depends_on:
      - agent-gateway
      - ai-mcp
    networks:
      - modernization-network
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/8081' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # 🟢 Green Agent - Orchestrator
  green-agent:
    build:
      context: ./green_agent
      dockerfile: Dockerfile
    container_name: green-agent
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=us-east-1
      - PURPLE_AGENT_URL=http://purple-agent:8081/solve
      - AGENT_GATEWAY_URL=http://agent-gateway:8090
      - AGENT_ID=green_agent
      - AGENT_API_KEY=${AGENT_API_KEY:-green-agent-dev-key-change-in-prod}
    depends_on:
      - agent-gateway
      - s3-mcp
      - ai-mcp
      - cobol-mcp
      - rust-mcp
      - purple-agent
    networks:
      - modernization-network
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/8080' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  modernization-network:
    driver: bridge
